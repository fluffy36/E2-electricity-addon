@name addons/electricity/Logic stuff/NOT Gate
@inputs Input
@outputs Out
@persist 
@strict
@model models/hunter/blocks/cube075x075x025.mdl
if(first()){
    enableConstraintUndo(0)
    entity():noCollideAll(0)
    entity():setMaterial("phoenix_storms/wire/pcb_green")
    if(!entity():isConstrained()){
        entity():weld(world())
    }
    runOnTick(1)
}

if(Input==1|Input==0){
    if(!Input){
        Out=1
    }else{
        Out=0
    }
}
