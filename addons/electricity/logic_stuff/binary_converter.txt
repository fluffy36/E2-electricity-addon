@name addons/electricity/Logic stuff/Binary converter
@inputs Terminal
@outputs Out
@persist 
@strict
@model models/hunter/blocks/cube075x075x025.mdl
enableConstraintUndo(0)
entity():noCollideAll(0)
entity():setMaterial("phoenix_storms/wire/pcb_green")
if(!entity():isConstrained()){
    entity():weld(world())
}

if(changed(Terminal)){
    if(Terminal>0){
        Out=1
    }
    if(Terminal==0){
        Out=0
    }
}


